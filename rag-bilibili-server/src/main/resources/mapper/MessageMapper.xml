<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.ragbilibili.mapper.MessageMapper">

    <resultMap id="BaseResultMap" type="com.example.ragbilibili.entity.Message">
        <id column="id" property="id"/>
        <result column="session_id" property="sessionId"/>
        <result column="role" property="role"/>
        <result column="content" property="content"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectBySessionId" resultMap="BaseResultMap">
        SELECT id, session_id, role, content, create_time
        FROM message
        WHERE session_id = #{sessionId}
        ORDER BY create_time ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO message (session_id, role, content, create_time)
        VALUES (#{sessionId}, #{role}, #{content}, #{createTime})
    </insert>

    <delete id="deleteBySessionIds">
        DELETE FROM message
        WHERE session_id IN
        <foreach collection="sessionIds" item="sessionId" open="(" separator="," close=")">
            #{sessionId}
        </foreach>
    </delete>

    <delete id="deleteBySessionId">
        DELETE FROM message WHERE session_id = #{sessionId}
    </delete>

</mapper>
